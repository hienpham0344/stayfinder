spring:
  application:
    name: stayfinder

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.wkdynklnimdizzlkbfkm.supabase.co:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:Quanghuy3321}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true



